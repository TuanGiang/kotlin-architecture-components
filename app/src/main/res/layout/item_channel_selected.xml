<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="channel"
            type="com.giangnt.kidtube.model.Channel" />

        <variable
            name="callBack"
            type="com.giangnt.kidtube.search.ChannelSearchClickCallBack" />
    </data>

    <android.support.constraint.ConstraintLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:onClick="@{()->callBack.onRemove(channel)}"
        android:paddingBottom="8dp">

        <ImageView
            android:id="@+id/imgChannel"
            android:layout_width="@dimen/channel_search_thumbnail_width"
            android:layout_height="0dp"
            android:layout_marginEnd="8dp"
            android:layout_marginStart="8dp"
            android:layout_marginTop="8dp"
            android:scaleType="fitXY"
            app:imageUrl="@{channel.thumbnail}"
            app:layout_constraintDimensionRatio="1:1"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />


        <TextView
            android:id="@+id/tvChannelName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:ellipsize="end"
            android:maxLines="2"
            android:text="@{channel.title}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="@+id/imgChannel"
            app:layout_constraintStart_toStartOf="@+id/imgChannel"
            app:layout_constraintTop_toBottomOf="@+id/imgChannel" />

    </android.support.constraint.ConstraintLayout>
</layout>
